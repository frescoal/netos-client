import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#0065c0',
    },
    secondary: {
      main: '#6Fbb6a',
    },
  },
});
export default theme;

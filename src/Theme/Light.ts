import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#006300',
    },
    secondary: {
      main: '#222eAA',
    },
  },
});
export default theme;

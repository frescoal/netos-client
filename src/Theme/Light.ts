import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#c06300',
    },
    secondary: {
      main: '#f62e43',
    },
  },
});
export default theme;
